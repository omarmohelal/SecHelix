{
  "schema_version": "1.0.0",
  "name": "Example company policy — customize privately",
  "blocking_severities": ["CRITICAL", "HIGH"],
  "require_independent_verification_for": ["CRITICAL", "HIGH"],
  "require_regression_for": ["CRITICAL", "HIGH"],
  "allow_accepted_risk": true,
  "accepted_risk_required_fields": ["reason", "approver", "approved_at", "expires_at", "ticket"],
  "integrity_critical_unknown_outcome": "BLOCKED",
  "required_tools": ["semgrep", "osv-scanner"],
  "forbidden_deployment_states": ["MIGRATION_DRIFT", "ROLLBACK_UNAVAILABLE"],
  "severity_overrides": {"SHX-EXAMPLE-PII": "HIGH"}
}
