# Recommended GitHub metadata

## Description

Evidence-first multi-agent AppSec: 546 hypotheses, independent verification,
safe tool adapters, and regression-backed reports.

## Topics

`appsec`, `agent-skills`, `security-audit`, `application-security`,
`multi-agent`, `security-testing`, `sarif`, `semgrep`, `codeql`,
`business-logic`, `authorization`, `mcp-security`, `devsecops`, `python`

## Social preview

Use `assets/readme-hero.svg` for repository documentation. If GitHub requires a
raster social preview, use an intentionally exported 1280×640 version rather
than any private website source asset.

These values are recommendations until a maintainer deliberately applies them
in GitHub settings.
